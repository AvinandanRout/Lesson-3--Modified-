class Rectangle():
    def _init_(self, 1, w):
        self.length = 1
        self.width = while

        def rectagle_area(self):
            return self.length*self.width
        

newRectangle = Rectangle(12, 10)
print("Dimention of Rectagle - Length : %d width : %d" % (newRectengle.Length, newRectangle.width))
print("Area of Rectangle :", newRectangle.rectangle_area())